export type FoundSquare = {
  category: string;
  words: string;
  mode: string;
}

export type BoardSquare = {
  id: number;
  word: string;
  selected: boolean;
  mode: string;
  category: string;
}

